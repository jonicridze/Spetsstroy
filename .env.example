APP_ORIGIN=http://localhost:8788
HOST=127.0.0.1
PORT=8788
DATA_DIR=./data
# NODE_ENV=production
# Create the first administrator with npm run admin:create, then remove password.
# SST_ADMIN_EMAIL=
# SST_ADMIN_NAME=
# SST_ADMIN_PASSWORD=
# Optional email notifications. Leads are saved even without SMTP.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=
# NOTIFY_EMAIL=
# BACKUP_DIR=./backups
