import {mainNavigation,overview,contactStrip,capabilities,suppliers,projectsLanding,newsLanding,homeProjects,homeNews} from './architecture.mjs';
import {sections,principles,faq} from './content.mjs';
import {list,db} from './db.mjs';
import {origin} from './security.mjs';
export const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const lines=x=>esc(x).replace(/\n/g,'<br>');
const paragraphs=x=>String(x||'').split(/\n\s*\n/).filter(Boolean).map(t=>'<p>'+lines(t)+'</p>').join('');
const arrow='<span aria-hidden="true">↗</span>';
const link=(url,title,cls='text-link')=>`<a class="${cls}" href="${esc(url)}">${esc(title)} ${arrow}</a>`;
export function photo(url,alt,{eager=false,cls='',transition=''}={}){if(!url)return '';let srcset='';if(url.startsWith('/media/')){const media=db.prepare('SELECT variants FROM media WHERE archived=0 AND variants LIKE ?').get('%'+url+'%');if(media){const variants=JSON.parse(media.variants);srcset=' srcset="'+Object.entries(variants).map(([w,u])=>esc(u)+' '+w+'w').join(',')+'" sizes="(max-width: 760px) 100vw, 85vw"';}}return `<img src="${esc(url)}" alt="${esc(alt)}" ${eager?'fetchpriority="high"':'loading="lazy"'} decoding="async" class="${cls}"${srcset}${transition?` style="view-transition-name:${esc(transition)}"`:''}>`;}
const label=(n,t,end='')=>`<div class="section-label"><span>${n} / ${esc(t)}</span><span>${esc(end)}</span></div>`;
function navItems(s){return mainNavigation;}
function layout(content,s,{title=s.seoTitle,description=s.seoDescription,path='/',image=s.ogImage,dark=false,preview=false}={}){const nav=navItems(s);return `<!doctype html><html lang="ru"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#171a1e"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${origin()+esc(path)}"><meta property="og:type" content="website"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${origin()+esc(path)}">${image?`<meta property="og:image" content="${origin()+esc(image)}">`:''}${preview?'<meta name="robots" content="noindex,nofollow">':''}<link rel="icon" href="${esc(s.favicon)}"><link rel="stylesheet" href="/experience.css"><link rel="stylesheet" href="/architecture.css"><link rel="stylesheet" href="/assets/vendor/leaflet/leaflet.css"><script type="importmap">{"imports":{"three":"/assets/vendor/three.module.js"}}</script></head><body class="${dark?'dark-top':''}"><a class="skip-link" href="#main">Перейти к содержанию</a><div class="scroll-progress" aria-hidden="true"></div>${preview?'<aside class="preview-banner">Предпросмотр. Материал ещё не обязательно опубликован. <a href="/admin">В редактор ↗</a></aside>':''}
<header class="header"><a href="/" class="brand" aria-label="Спецстройтехнологии — главная"><img src="${esc(s.logo)}" alt="" width="52" height="52"><span>СПЕЦСТРОЙ<small>ТЕХНОЛОГИИ</small></span></a><nav aria-label="Основная навигация">${nav.map(n=>`<a href="${n.url}" ${n.url===path?'aria-current="page"':''}>${esc(n.label)}</a>`).join('')}</nav><div class="header-actions">${link('/contacts#brief',s.cta,'header-cta')}<button class="menu-toggle" aria-label="Открыть меню" aria-expanded="false" aria-controls="site-menu"><span></span><span></span></button></div></header>
<dialog id="site-menu" class="site-menu"><div class="menu-head"><span>${esc(s.brand)}</span><button class="menu-close" aria-label="Закрыть меню">Закрыть ×</button></div><nav aria-label="Все разделы">${nav.map((n,i)=>`<a href="${n.url}"><small>${String(i+1).padStart(2,'0')}</small>${esc(n.label)}<span>↗</span></a>`).join('')}</nav><div class="menu-contact"><a href="mailto:${esc(s.email)}">${esc(s.email)}</a><a href="tel:${s.phone.replace(/[^+\d]/g,'')}">${esc(s.phone)}</a></div></dialog>
<main id="main">${content}</main>
<footer class="footer">${label('ССТ','КОНТАКТ С БУДУЩИМ')}<div class="footer-top"><h2>Большое начинается<br>с <em>разговора.</em></h2>${link('/contacts#brief','Обсудить проект','circle-link')}</div><div class="footer-grid"><a class="footer-mail" href="mailto:${esc(s.email)}">${esc(s.email)} ↗</a><div><a href="tel:${s.phone.replace(/[^+\d]/g,'')}">${esc(s.phone)}</a><address>${esc(s.address)}</address></div><nav>${nav.map(n=>`<a href="${n.url}">${esc(n.label)}</a>`).join('')}</nav></div><div class="footer-wordmark" aria-hidden="true">СПЕЦСТРОЙ<br><span>ТЕХНОЛОГИИ</span></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} ${esc(s.legalName)}</span><a href="/privacy">Конфиденциальность</a><button id="motion-toggle" aria-pressed="true">Анимация: включена</button><a href="#main">Наверх ↑</a></div></footer><div id="hover-cursor" aria-hidden="true">ОТКРЫТЬ ↗</div><script type="module" src="/app.js"></script><script src="/assets/vendor/leaflet/leaflet.js" defer></script><script type="module" src="/architecture.js"></script></body></html>`;}
function emblem(){return `<section class="emblem-scene" id="form">${label('02','ФОРМА, В КОТОРОЙ ЕСТЬ СМЫСЛ','ССТ / ИНТЕРАКТИВНЫЙ ЗНАК')}<div class="emblem-layout"><div class="emblem-copy"><h2>За каждой<br>формой —<br><em>продуманная<br>структура.</em></h2><p>Прикоснитесь к форме.<br>Вращайте. Отпустите.<br>Она вернётся.</p><span class="tech-label">ТОЧНОСТЬ В КАЖДОМ СОЕДИНЕНИИ</span></div><div class="hero-art"><div class="art-grid"></div><div class="orbit"></div><div class="model-shadow"></div><div id="logo-stage" tabindex="0" role="img" aria-label="Объёмный знак ССТ: вращайте мышью или пальцем. Стрелки — вращение, Escape — возврат."><img class="logo-fallback" src="/assets/emblem.svg" alt="Фирменный знак ССТ"></div><div class="model-caption"><span>↔ Вращайте знак</span><span id="rotation-value">000°</span><button id="reset-logo" aria-label="Вернуть логотип в исходное положение">↺</button></div></div></div></section>`;}
function principleCards(){return `<section class="principles-section section" id="approach">${label('03','ПОДХОД','ТОЧНОСТЬ. СТРУКТУРА. РЕЗУЛЬТАТ.')}<div class="section-heading"><h2>Продумано.<br>Связано.<br><em>Построено.</em></h2><p>Большая задача становится понятнее,<br>когда у каждого решения есть основание.</p></div><div class="transform-cards">${principles.map((p,i)=>`<article class="transform-card" style="--index:${i}"><div class="card-plane"><span class="card-index">0${i+1}</span><div class="card-lines" aria-hidden="true"></div><span class="card-kicker">${esc(p.subtitle)}</span><h3>${esc(p.title)}</h3><button class="card-toggle" aria-expanded="false" aria-controls="principle-${i}">Раскрыть принцип <span>＋</span></button></div><div class="card-reverse" id="principle-${i}" inert><span>0${i+1} / ${esc(p.subtitle)}</span><h3>${esc(p.title)}</h3><p>${esc(p.text)}</p>${link('/company#principles','О нашем подходе')}</div></article>`).join('')}</div></section>`;}
function projectCards(items,kind='projects'){const route=sections[kind].route;return `<div class="editorial-projects">${items.map((p,i)=>`<article class="project-entry entry-${i%4}"><a class="project-image" href="/${route}/${p.slug}" data-cursor="ОТКРЫТЬ ↗">${photo(p.image,p.caption||p.title,{transition:'item-'+p.id})}<span class="project-open">Смотреть ${kind==='projects'?'проект':'материал'} ↗</span></a><div class="project-meta"><span>${String(i+1).padStart(2,'0')} / ${esc(p.region||p.date||sections[kind].singular)}</span><span>${esc(p.year)}</span></div><h3><a href="/${route}/${p.slug}">${esc(p.title)}</a></h3>${p.excerpt?'<p>'+esc(p.excerpt)+'</p>':''}</article>`).join('')}</div>`;}
function servicesCards(items){return `<div class="service-cards">${items.map((p,i)=>`<article class="service-card transform-card"><div class="card-plane">${photo(p.image,p.caption||p.title)}<span class="card-index">${String(i+1).padStart(2,'0')}</span><h3>${esc(p.title)}</h3><button class="card-toggle" aria-expanded="false" aria-controls="service-${p.id}">Подробнее <span>＋</span></button></div><div class="card-reverse" id="service-${p.id}" inert><h3>${esc(p.title)}</h3><p>${esc(p.excerpt||p.body.slice(0,300))}</p>${link('/competencies/'+p.slug,'Изучить направление')}</div></article>`).join('')}</div>`;}
function home(s){const projects=list('projects').filter(p=>p.featured),services=list('services'),news=list('news').slice(0,3),stats=list('stats');const parts={
 intro:`<section class="intro section">${label('01','ИНЖЕНЕРНЫЙ ВЗГЛЯД')}<div class="intro-layout"><h2>Масштаб —<br>в замысле.<br><em>Качество —<br>в деталях.</em></h2><div>${paragraphs(s.companyText)}${link('/company','Узнать компанию')}</div></div></section>`,form:emblem(),principles:principleCards(),
 projects:projects.length?`<section class="section projects-section">${label('04','ПРОЕКТЫ')}<h2>Решения,<br><em>обретшие форму.</em></h2>${projectCards(projects.slice(0,4))}${link('/projects','Все проекты')}</section>`:'',
 services:services.length?`<section class="section">${label('05','КОМПЕТЕНЦИИ')}<h2>Задачи разного масштаба.<br><em>Единая логика.</em></h2>${servicesCards(services)}</section>`:'',
 stats:stats.length?`<section class="section stats-section">${label('06','В ЦИФРАХ')}<div class="stats-grid">${stats.map(x=>`<div><strong>${esc(x.value)}<small>${esc(x.unit)}</small></strong><p>${esc(x.title)}</p></div>`).join('')}</div></section>`:'',
 news:news.length?`<section class="section">${label('07','НОВОСТИ')}<h2>Компания<br><em>в движении.</em></h2>${projectCards(news,'news')}${link('/news','Все новости')}</section>`:''};
 return layout(`<section class="cinema-hero"><div class="hero-media">${photo(s.heroImage,s.heroCaption,{eager:true})}${s.heroVideo?`<video muted loop playsinline preload="none" poster="${esc(s.heroImage)}" data-autoplay><source src="${esc(s.heroVideo)}"></video>`:''}</div><div class="hero-eyebrow"><span>СТРОИТЕЛЬНАЯ КОМПАНИЯ</span><span>ССТ / ${new Date().getFullYear()}</span></div><div class="hero-survey" aria-hidden="true"><span>59° / ССТ</span><i></i><span>ТОЧНОСТЬ В ДЕТАЛЯХ</span></div><h1>${s.heroTitle.split('\n').map((x,i)=>`<span class="hero-line line-${i}"><span class="hero-reveal">${esc(x)}</span></span>`).join('')}</h1><div class="hero-baseline"><p>${lines(s.heroSubtitle)}</p><a href="#intro" class="scroll-down">Исследовать <span>↓</span></a><span class="hero-caption">${esc(s.heroCaption)}</span></div><div class="hero-index"><a href="/company">01 / О компании ↗</a><a href="/capabilities">02 / Наши возможности ↗</a><a href="/projects">03 / Проекты ↗</a></div></section>${contactStrip(s)}<div id="intro">${s.homeSections.filter(k=>!s.hiddenSections.includes(k)).map(k=>({services:overview(),projects:homeProjects(),news:homeNews()})[k]||parts[k]||'').join('')}</div>`,s,{dark:true});}
function company(s){return layout(`<section class="page-intro section">${label('01','КОМПАНИЯ')}<h1 class="split-title">СПЕЦ<br><span>СТРОЙ</span><br>ТЕХНОЛОГИИ</h1><div class="intro-layout company-copy"><h2>Сложное.<br><em>В простом.</em></h2><div>${paragraphs(s.companyText)}<p>Точность. Структура. Результат.</p></div></div></section><section class="wide-image">${photo(s.heroImage,s.heroCaption)}<span>${esc(s.heroCaption)}</span><h2>Внимание к деталям.<br>В масштабе целого.</h2></section><section class="section scroll-story" id="principles">${label('02','ПРИНЦИПЫ')}<div class="story-layout"><div class="story-sticky"><h2>Продумано.<br>Связано.<br><em>Построено.</em></h2><img src="/assets/emblem.svg" alt="Знак ССТ" width="240" height="240"></div><div>${principles.map((p,i)=>`<article class="story-step"><span>0${i+1}</span><h3>${esc(p.title)}</h3><p>${esc(p.text)}</p></article>`).join('')}</div></div></section><section class="section company-conclusion"><p>За каждой формой —<br><em>продуманная структура.</em></p>${link('/contacts','Начать разговор')}</section>`,s,{path:'/company',title:'Компания — '+s.brand});}
function brief(s){return `<section class="section brief-section" id="brief">${label('02','ОБСУДИТЬ ПРОЕКТ')}<div class="brief-grid"><div><h2>У вашего проекта<br><em>есть отправная точка.</em></h2><p>Расскажите, что планируете построить.<br>Даже если пока есть только идея.</p><div class="faq">${faq.map(([q,a])=>`<details><summary>${esc(q)}<span>＋</span></summary><p>${esc(a)}</p></details>`).join('')}</div></div><form id="project-form" enctype="multipart/form-data"><div class="form-row"><label>Имя *<input name="name" autocomplete="name" required maxlength="100"></label><label>Компания<input name="company" autocomplete="organization" maxlength="200"></label><label>Телефон<input name="phone" type="tel" autocomplete="tel" maxlength="50"></label><label>Email<input name="email" type="email" autocomplete="email" maxlength="200"></label></div><p class="form-hint">Укажите телефон или email для ответа.</p><label>Тип проекта<input name="projectType" maxlength="200" placeholder="Например, производственное здание"></label><label>Сообщение *<textarea name="message" rows="4" required maxlength="10000" placeholder="Расположение, стадия, задачи и пожелания"></textarea></label><label class="file-drop" id="file-drop"><span>Перетащите документы<br>или <u>выберите файлы</u></span><small>До 5 файлов · до 8 МБ каждый<br>PDF, DOC/DOCX, XLS/XLSX, JPG, PNG, WebP</small><input type="file" name="files" multiple accept=".pdf,.doc,.docx,.xls,.xlsx,.jpg,.jpeg,.png,.webp" aria-label="Документы к проекту"></label><ul id="selected-files" class="selected-files"></ul><label class="consent"><input type="checkbox" name="consent" value="yes" required><span>Согласен на обработку данных моего обращения согласно <a href="/privacy" target="_blank" rel="noopener">политике конфиденциальности</a>.</span></label><div class="honeypot" aria-hidden="true"><label>Сайт<input name="website" tabindex="-1" autocomplete="off"></label></div><button type="submit" class="button">Отправить обращение ${arrow}</button><p id="form-status" role="status" aria-live="polite"></p><p class="form-hint">Можно также написать на <a href="mailto:${esc(s.email)}">${esc(s.email)}</a>.</p></form></div></section>`;}
function contacts(s){return layout(`<section class="page-intro section contacts-intro">${label('01','КОНТАКТЫ')}<h1>Давайте<br><em>строить диалог.</em></h1><a class="contact-mail" href="mailto:${esc(s.email)}">${esc(s.email)} ↗</a><div class="contact-columns"><div><span class="tech-label">ТЕЛЕФОН</span><a class="phone" href="tel:${s.phone.replace(/[^+\d]/g,'')}">${esc(s.phone)}</a>${s.hours?'<p>'+esc(s.hours)+'</p>':''}</div><div><span class="tech-label">АДРЕС</span><address>${esc(s.address)}</address>${link(s.mapUrl,'Открыть на карте')}</div><div><span class="tech-label">РЕКВИЗИТЫ</span><p>${esc(s.legalName)}<br>ИНН ${esc(s.inn)}<br>КПП ${esc(s.kpp)}<br>ОГРН ${esc(s.ogrn)}</p><details><summary>Юридический адрес <span>＋</span></summary><p>${esc(s.legalAddress)}</p></details></div></div><div class="map-panel"><iframe loading="lazy" title="Расположение офиса: Русско-Высоцкое, Промышленная, 1" src="https://yandex.ru/map-widget/v1/?text=${encodeURIComponent(s.address)}" referrerpolicy="no-referrer"></iframe><div>${esc(s.address)} ${link(s.mapUrl,'Проложить маршрут')}</div></div></section>${brief(s)}`,s,{path:'/contacts',title:'Контакты — '+s.brand});}
function collection(kind,s){const items=list(kind);if(!items.length)return null;const c=sections[kind];let body;if(kind==='services')body=servicesCards(items);else if(kind==='equipment')body=`<div class="equipment-rail" tabindex="0" aria-label="Каталог техники, прокручивайте горизонтально">${items.map(p=>`<article>${photo(p.image,p.caption||p.title)}<span class="tech-label">${esc(p.category)}</span><h2>${esc(p.title)}</h2><p>${esc(p.manufacturer)} ${esc(p.model)} ${esc(p.year)}</p>${link('/equipment/'+p.slug,'Характеристики')}</article>`).join('')}</div>`;else if(kind==='vacancies')body=`<div class="vacancy-list">${items.map(p=>`<a href="/career/${p.slug}"><span>${esc(p.city)}</span><h2>${esc(p.title)}</h2><span>${esc(p.employment)} ↗</span></a>`).join('')}</div>`;else if(kind==='documents')body=`<div class="vacancy-list">${items.map(p=>`<a href="${esc(p.document||'/documents/'+p.slug)}"><h2>${esc(p.title)}</h2><span>Открыть документ ↗</span></a>`).join('')}</div>`;else body=projectCards(items,kind);
const regions=[...new Set(items.map(x=>x.region).filter(Boolean))];return layout(`<section class="page-intro section">${label('01',c.label.toUpperCase())}<h1>${esc(c.label)}<span class="title-count">${String(items.length).padStart(2,'0')}</span></h1>${items.length>6?'<label class="collection-search">Поиск по названию<input type="search" id="collection-search" placeholder="Введите название"></label>':''}${body}${kind==='projects'&&regions.length?`<section class="geography"><h2>География<br><em>проектов.</em></h2><div>${regions.map(region=>`<details><summary>${esc(region)}<span>＋</span></summary>${items.filter(x=>x.region===region).map(x=>link('/projects/'+x.slug,x.title)).join('')}</details>`).join('')}</div></section>`:''}</section>`,s,{path:'/'+c.route,title:c.label+' — '+s.brand});}
function detail(p,s,preview=false){const c=sections[p.kind];if(!c?.route)return null;const specs=[['Регион',p.region],['Место',p.location],['Год',p.year],['Тип объекта',p.objectType],['Категория',p.category],['Производитель',p.manufacturer],['Модель',p.model],['Город',p.city],['Занятость',p.employment]].filter(x=>x[1]);const other=list(p.kind).filter(x=>x.id!==p.id).slice(0,2);return layout(`${p.demo?'<aside class="demo-detail">ДЕМОНСТРАЦИОННЫЙ ПРИМЕР — НЕ РЕАЛЬНЫЙ ОБЪЕКТ ССТ</aside>':''}<section class="detail-hero ${p.image?'with-image':''}">${p.image?photo(p.image,p.caption||p.title,{eager:true,transition:'item-'+p.id}):''}<div><a class="back-link" href="/${c.route}">← ${esc(c.label)}</a><span class="tech-label">${esc(p.region||p.date||c.singular)}</span><h1>${esc(p.title)}</h1></div></section><section class="section detail-body">${specs.length?`<dl class="spec-grid">${specs.map(([k,v])=>`<div><dt>${esc(k)}</dt><dd>${esc(v)}</dd></div>`).join('')}</dl>`:''}<div class="body-copy">${p.excerpt?'<p class="lead-text">'+esc(p.excerpt)+'</p>':''}${paragraphs(p.body)}${p.work?'<h2>Выполненные работы</h2>'+paragraphs(p.work):''}${p.specifications?'<h2>Характеристики</h2>'+paragraphs(p.specifications):''}${p.requirements?'<h2>Требования</h2>'+paragraphs(p.requirements):''}${p.conditions?'<h2>Условия</h2>'+paragraphs(p.conditions):''}${p.contact?'<h2>Контакт для отклика</h2>'+paragraphs(p.contact):''}${p.document?link(p.document,'Открыть документ','button'):''}</div></section>${p.gallery?.length?`<section class="detail-gallery">${p.gallery.map((img,i)=>`<figure class="gallery-${i%3}">${photo(img,p.title+' — фотография '+(i+1))}<figcaption>${String(i+1).padStart(2,'0')} / ${esc(p.title)}</figcaption></figure>`).join('')}</section>`:''}${p.video?`<section class="section"><video class="project-video" controls preload="none" poster="${esc(p.image)}"><source src="${esc(p.video)}"></video></section>`:''}${other.length?`<section class="section">${label('ДАЛЕЕ',c.label.toUpperCase())}${projectCards(other,p.kind)}</section>`:''}`,s,{path:'/'+c.route+'/'+p.slug,title:p.seoTitle||p.title+' — '+s.brand,description:p.seoDescription||p.excerpt||s.seoDescription,image:p.image||s.ogImage,dark:!!p.image,preview});}
function privacy(s){return layout(`<section class="section page-intro"><h1>Конфиденциальность</h1><div class="body-copy"><p>Оператор обращений: ${esc(s.legalName)}. Адрес: ${esc(s.legalAddress)}. Контакт по вопросам данных: <a href="mailto:${esc(s.email)}">${esc(s.email)}</a>.</p><h2>Данные обращения</h2><p>При заполнении формы вы передаёте указанные вами имя, компанию, телефон, email, описание проекта и приложенные документы. Эти данные используются для рассмотрения обращения и обратной связи.</p><h2>Хранение и доступ</h2><p>Обращения сохраняются на сервере. Доступ к ним и вложениям предоставляется только уполномоченным администраторам. Для административного входа используется необходимый сессионный cookie. Публичная часть не использует рекламные cookie; настройка анимации может сохраняться только на вашем устройстве.</p><h2>Ваш запрос</h2><p>Для уточнения, удаления данных или отзыва согласия свяжитесь с оператором по указанному email. Не прикладывайте документы, не относящиеся к обсуждаемому проекту. Сроки хранения определяются целью обращения и применимыми требованиями; организационный порядок должен быть утверждён оператором до начала приёма обращений.</p><h2>Внешние сервисы</h2><p>Шрифт загружается из Google Fonts. На странице контактов карта загружается с Яндекс Карт. На карте объектов используются OpenStreetMap и библиотека Leaflet. Эти сервисы получают стандартные технические сведения HTTP-запроса при загрузке их ресурсов. Отправка самой формы выполняется на сервер сайта.</p></div></section>`,s,{path:'/privacy',title:'Конфиденциальность — '+s.brand});}
export function notFound(s){return layout(`<section class="not-found section"><span>404 / СТРАНИЦА НЕ НАЙДЕНА</span><h1>Форма потеряла<br><em>структуру.</em></h1>${link('/','Вернуться на главную','button')}</section>`,s,{title:'Страница не найдена — '+s.brand});}
export function renderPage(path,s,{preview}={}){if(preview)return detail(preview,s,true);if(path==='/')return home(s);if(path==='/company')return company(s);if(path==='/capabilities')return layout(capabilities(s),s,{path,title:'Наши возможности — '+s.brand});if(path==='/suppliers')return layout(suppliers(s),s,{path,title:'Поставщикам — '+s.brand});if(path==='/projects')return layout(projectsLanding(),s,{path,title:'Проекты — '+s.brand});if(path==='/news')return layout(newsLanding(),s,{path,title:'Новости — '+s.brand});if(path==='/contacts')return contacts(s);if(path==='/privacy')return privacy(s);const[,route,slug]=path.split('/'),kind=Object.keys(sections).find(k=>sections[k].route===route);if(!kind)return null;if(!slug)return collection(kind,s);const p=list(kind).find(x=>x.slug===slug);return p?detail(p,s):null;}
export function renderAdmin(){return `<!doctype html><html lang="ru"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>ССТ — управление сайтом</title><link rel="icon" href="/assets/emblem.svg"><link rel="stylesheet" href="/admin.css"></head><body><div id="admin-app"><main class="login-shell"><img src="/assets/emblem.svg" width="80" height="80" alt="ССТ"><h1>Управление сайтом</h1><p>Вход для сотрудников компании</p><form id="login-form"><label>Email<input name="email" type="email" autocomplete="username" required></label><label>Пароль<input name="password" type="password" autocomplete="current-password" required></label><button>Войти</button><p id="login-error" role="alert"></p></form><a href="/">← На сайт</a></main></div><div id="toast" role="status" aria-live="polite"></div><script type="module" src="/admin.js"></script></body></html>`;}
